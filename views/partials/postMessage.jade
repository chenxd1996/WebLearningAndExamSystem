div(class="content-container")
    div(class="col-xs-10 col-xs-offset-1 posRelative bg-white pt-10 pb-10")
        label 通知课程
        select(class="form-control mb-10" ng-model="message.courseSelected" ng-options="course.name for course in courses" required)
        label 通知标题
        input(type="text" class="form-control mb-10" placeholder="请输入通知的标题" ng-model="message.title")
        label 通知正文
        div(class="mb-10" editor)
        label(class="checkbox-inline")
            input(type="checkbox" ng-model="message.includeTeacher")
            span 是否通知课程其他教师？
        button(class="btn btn-primary pull-right mt-10" ng-click="submit()")
            i(class="fa fa-fw fa-send-o mr-5")
            span 发布
//div(class="panel panel-info")
    div(class="panel-heading")
        h3 发布通知
    div(class="panel-body panelBody")
        div(class="col-xs-8 col-xs-offset-2 posRelative")
            label 通知课程
            select(class="form-control mb-10" ng-model="message.courseSelected" ng-options="course.name for course in courses" required)
            label 通知标题
            input(type="text" class="form-control mb-10" placeholder="请输入通知的标题" ng-model="message.title")
            label 通知正文
            div(class="mb-10" editor)
            label(class="checkbox-inline")
                input(type="checkbox" ng-model="message.includeTeacher")
                span 是否通知课程其他教师？
            button(class="btn btn-primary pull-right mt-10" ng-click="submit()")
                i(class="fa fa-fw fa-send-o mr-5")
                span 发布

